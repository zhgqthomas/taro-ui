<block>
    <view class="page">
        <docs-header title="Badge 徽标" __triggerObserer="{{ _triggerObserer }}"></docs-header>
        <view class="doc-body">
            <view class="panel">
                <view class="panel__title">数字</view>
                <view class="panel__content">
                    <view class="badge-item">
                        <view class="subitem">
                            <at-badge value="10" __triggerObserer="{{ _triggerObserer }}" maxValue="{{99}}">
                                <at-button size="small" __triggerObserer="{{ _triggerObserer }}" circle="{{true}}">按钮</at-button>
                            </at-badge>
                        </view>
                        <view class="subitem">
                            <at-badge value="100" __triggerObserer="{{ _triggerObserer }}" maxValue="{{99}}">
                                <at-button size="small" __triggerObserer="{{ _triggerObserer }}">按钮</at-button>
                            </at-badge>
                        </view>
                    </view>
                </view>
            </view>
            <view class="panel">
                <view class="panel__title">小红点</view>
                <view class="panel__content">
                    <view class="badge-item">
                        <view class="subitem">
                            <at-badge dot="{{true}}" __triggerObserer="{{ _triggerObserer }}">
                                <at-button size="small" __triggerObserer="{{ _triggerObserer }}" circle="{{true}}">按钮</at-button>
                            </at-badge>
                        </view>
                        <view class="subitem">
                            <at-badge dot="{{true}}" __triggerObserer="{{ _triggerObserer }}">
                                <at-button size="small" __triggerObserer="{{ _triggerObserer }}">按钮</at-button>
                            </at-badge>
                        </view>
                    </view>
                </view>
            </view>
            <view class="panel">
                <view class="panel__title">文本</view>
                <view class="panel__content">
                    <view class="badge-item">
                        <view class="subitem">
                            <at-badge value="NEW" __triggerObserer="{{ _triggerObserer }}">
                                <at-button size="small" __triggerObserer="{{ _triggerObserer }}" circle="{{true}}">按钮</at-button>
                            </at-badge>
                        </view>
                        <view class="subitem">
                            <at-badge value="NEW" __triggerObserer="{{ _triggerObserer }}">
                                <at-button size="small" __triggerObserer="{{ _triggerObserer }}">按钮</at-button>
                            </at-badge>
                        </view>
                    </view>
                </view>
            </view>
            <view class="panel">
                <view class="panel__title">省略号</view>
                <view class="panel__content">
                    <view class="badge-item">
                        <view class="subitem">
                            <at-badge value="{{dot}}" __triggerObserer="{{ _triggerObserer }}">
                                <at-button size="small" __triggerObserer="{{ _triggerObserer }}" circle="{{true}}">按钮</at-button>
                            </at-badge>
                        </view>
                        <view class="subitem">
                            <at-badge value="{{dot}}" __triggerObserer="{{ _triggerObserer }}">
                                <at-button size="small" __triggerObserer="{{ _triggerObserer }}">按钮</at-button>
                            </at-badge>
                        </view>
                    </view>
                </view>
            </view>
        </view>
    </view>
</block>